const frutas = ["maça", "banana", "uva", "pera"];
let cont = 0;

// Exemplo 1

    console.log("Exemplo 1 \n")

    //  length trás o tamanho da lista

    while(cont < frutas.length) {
        
        // $ - identifica uma variável
        console.log(`${cont} - ${frutas[cont]}`)
        cont++

    }