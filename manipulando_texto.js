let numFloat = 123.543;
let linguagens = ["Java", "C#", "Python"];
//                  0       1       2
let txt = "Estão chegando as provas";

// Convertendo a variavel 

    // .toString() - Função nativa do JS. Converte um número em script

        // console.log(typeof numFloat.toString());

        // Forma atualizada de converter 
            // console.log(typeof String(numFloat));

// A variavel linguagens é um array, um objeto

    // console.log(typeof linguagens);
    // console.log(linguagens[2]);

// .replace() - Função nativa que altera valores nas strings
console.log(txt.replace("provas", "avaliações"));