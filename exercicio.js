// Escreva um loop que exiba a frase abaixo no console 
// "Está é a X° vez que está frase é exibida no console"
//      - Substitua o "x" pela informação correta;
//      - A cada execução do loop, incrementar x em +1 até 10
//      - O loop deve parar quando x for igual ou superior a 10. Exibindo 10x o console


for (x=0 ; x<10; x++) {

    console.log(`Está é a ${x + 1}º que está frase é exibida no console.`)

}